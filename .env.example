# Frontend (browser-exposed)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1

# Frontend↔backend wiring (used by docker-compose)
FRONTEND_ORIGIN=http://localhost:3000
AUTH_BASE_URL=http://localhost:4000

# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Redis (compose service overrides this; set for local-without-compose dev)
REDIS_URL=redis://localhost:6379
REDIS_KEY_PREFIX=dotadata:v1

# Auth — must be at least 32 characters
AUTH_SECRET=change-me-to-a-32-plus-char-secret-string
STEAM_API_KEY=

# Optional integrations
N8N_CONTACT_WEBHOOK_URL=
BLOG_ADMIN_TOKEN=
COOKIE_DOMAIN=
